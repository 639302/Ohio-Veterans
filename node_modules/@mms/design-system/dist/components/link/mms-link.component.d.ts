import { LitElement } from 'lit';
import '../icon/mms-icon.component.js';
export type LinkSize = 'sm' | 'md' | 'lg';
export type LinkColor = 'standard' | 'neutral';
export type LinkUnderline = 'always' | 'hover' | 'none';
export type LinkState = 'default' | 'hover' | 'focus' | 'visited';
/**
 * Link component for navigation with USWDS-aligned colors.
 *
 * When `href` is provided, renders a semantic `<a>` element.
 * When `href` is omitted, renders styled text for use inside framework router wrappers.
 *
 * @element mms-link
 *
 * @prop {string} href - Navigation URL. When set, renders as `<a>`. When omitted, renders as styled span.
 * @prop {string} target - Where to open link (_self, _blank, etc.). Only applies when href is set.
 * @prop {string} label - Link text content
 * @prop {LinkSize} size - Text size: sm (body-sm), md (body-md), lg (body-lg)
 * @prop {LinkColor} link-color - Color treatment: standard (USWDS blue), neutral (inherit text color)
 * @prop {LinkUnderline} underline - Underline behavior: always, hover (default), none
 * @prop {boolean} disabled - Disabled state
 * @prop {LinkState} state - Preview state for Storybook demos
 * @prop {string} leftIcon - Icon name from registry for left position
 * @prop {string} rightIcon - Icon name from registry for right position
 */
export declare class MmsLink extends LitElement {
    href?: string;
    target?: string;
    label: string;
    size: LinkSize;
    linkColor: LinkColor;
    underline: LinkUnderline;
    disabled: boolean;
    state: LinkState;
    leftIcon: string;
    rightIcon: string;
    static styles: import("lit").CSSResult[];
    connectedCallback(): void;
    disconnectedCallback(): void;
    private _handleKeyDown;
    updated(changedProperties: Map<string, unknown>): void;
    /**
     * Compute icon size based on link size
     */
    private _getIconSize;
    render(): import("lit-html").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'mms-link': MmsLink;
    }
}
//# sourceMappingURL=mms-link.component.d.ts.map