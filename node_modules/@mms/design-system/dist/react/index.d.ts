/**
 * React wrapper components for @mms/design-system
 *
 * These wrappers use @lit/react to provide:
 * - TypeScript prop types
 * - React-style event handlers (onClick, onChange, etc.)
 * - Proper ref forwarding
 * - camelCase prop names
 *
 * Usage:
 *   import { MmsButton, MmsIcon } from '@mms/design-system/react';
 *
 *   <MmsButton variant="primary" label="Submit" onClick={() => handleClick()} />
 */
import { type EventName } from '@lit/react';
import { MmsButton as MmsButtonElement } from '../components/button/mms-button.component.js';
import { MmsIcon as MmsIconElement } from '../components/icon/mms-icon.component.js';
export type { ButtonVariant, ButtonSize, ButtonRoundness, ButtonState, ButtonColorScheme, } from '../components/button/mms-button.component.js';
/**
 * MmsButton - React wrapper for mms-button web component
 *
 * @example
 * ```tsx
 * import { MmsButton } from '@mms/design-system/react';
 *
 * <MmsButton
 *   variant="primary"
 *   label="Submit"
 *   leftIcon="check"
 *   onClick={() => console.log('clicked')}
 * />
 * ```
 */
export declare const MmsButton: import("@lit/react").ReactWebComponent<MmsButtonElement, {
    onClick: EventName<MouseEvent>;
    onFocus: EventName<FocusEvent>;
    onBlur: EventName<FocusEvent>;
}>;
/**
 * MmsIcon - React wrapper for mms-icon web component
 *
 * @example
 * ```tsx
 * import { MmsIcon } from '@mms/design-system/react';
 *
 * <MmsIcon name="house" size="lg" weight="bold" />
 * ```
 */
export declare const MmsIcon: import("@lit/react").ReactWebComponent<MmsIconElement, {
    onClick: EventName<MouseEvent>;
}>;
//# sourceMappingURL=index.d.ts.map